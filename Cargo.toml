[package]
name = "dsh-manager"
version = "0.1.0"
edition = "2024"

[dependencies]
slint = "1.18"
ureq = "3.4"
serde_json = "1"
semver = "1"

[build-dependencies]
slint-build = "1.18"

[profile.release]
opt-level = "s"
lto = true
strip = true
